meta {
  name: Events - Search with Empty Query
  type: http
  seq: 6
}

get {
  url: http://localhost:8081/api/events/search?q=
  body: none
  auth: none
}

tests {
  test("Empty search query should return 400", function() {
    expect(res.getStatus()).to.equal(400);
  });

  test("Should return error message", function() {
    const responseJson = res.getBody();
    expect(responseJson).to.have.property('message');
    expect(responseJson.message).to.equal('Search term is required');
  });
}